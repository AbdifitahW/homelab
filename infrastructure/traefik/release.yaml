---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 1m
  chartRef:
    kind: OCIRepository
    name: traefik
    namespace: flux-system
  timeout: 10m
  install:
    disableWait: true
  upgrade:
    disableWait: true
  rollback:
    disableWait: true
  values:
    persistence:
      enabled: true
      path: /step-ca
      size: 1Gi
    certificatesResolvers:
      homelab:
        acme:
          email: admin
          caServer: https://step-ca-step-certificates.step-ca.svc.cluster.local:9000/acme/acme/directory
          storage: /step-ca/acme.json
          certificatesDuration: 72
          tlsChallenge: true
    podSecurityContext:
      runAsGroup: 65532
      runAsNonRoot: true
      runAsUser: 65532
      fsGroup: 65532
      fsGroupChangePolicy: "OnRootMismatch"
