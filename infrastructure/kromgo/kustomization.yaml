configMapGenerator:
  - files:
      - config.yaml=./resources/config.yaml
    name: kromgo-cm
    namespace: monitoring
generatorOptions:
  disableNameSuffixHash: true
resources:
  - release.yaml
  - ocirepository.yaml
